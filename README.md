# Zone VSCode

Zone VSCode is a minimal extension shell that contributes `Zone: Open Panel`, opens a dark terminal-styled webview, and echoes prompts through a host/webview message round-trip. Run `npm install`, then press F5 in VSCode/VSCodium to launch an Extension Development Host and execute the command from the Command Palette.
